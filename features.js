// This file contains legacy functions that are no longer used.
// All functionality has been moved to extra_features.js

// Legacy function - no longer used
export function drawScore(ctx, score) {
    // Function removed - score is now displayed in HTML
}

// Legacy function - no longer used  
export function drawLives(ctx, lives) {
    // Function removed - lives are now displayed in HTML
}